---
import "../styles/prism.css";
---

<section class="max-w-7xl mx-auto px-4 py-16">
  <div class="mb-8">
    <h2 class="text-3xl font-bold text-white mb-4">
      Start Developing with MTA:SA
    </h2>
    <p class="text-gray-400">
      Create powerful multiplayer experiences with our Lua-based scripting
      system
    </p>
  </div>

  <div class="grid md:grid-cols-2 gap-8">
    <!-- Code Example Card -->
    <div
      class="bg-gray-900/50 backdrop-blur-md rounded-xl border border-white/10 overflow-hidden"
    >
      <div
        class="flex items-center justify-between px-4 py-2 bg-gray-800/50 border-b border-white/10"
      >
        <div class="flex items-center gap-2">
          <div class="w-3 h-3 rounded-full bg-red-500"></div>
          <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
          <div class="w-3 h-3 rounded-full bg-green-500"></div>
        </div>
        <span class="text-sm text-gray-400">racing_system.lua</span>
      </div>
      <div class="p-4">
        <pre
          class="text-sm text-gray-300 font-mono"><code class="language-lua typewriter-effect">{`-- Racing System Example
local races = {}

function createRace(name, checkpoints)
    local race = {
        name = name,
        checkpoints = checkpoints,
        players = {},
        state = "waiting"
    }
    
    table.insert(races, race)
    return #races
end

addEvent("onRaceStart", true)
addEventHandler("onRaceStart", root, function(raceId)
    if races[raceId] then
        races[raceId].state = "running"
        
        for player, data in pairs(races[raceId].players) do
            spawnPlayer(player, data.spawn)
            setElementFrozen(player, false)
            triggerClientEvent(player, "startRaceCountdown", player)
        end
    end
end)

function joinRace(player, raceId)
    if races[raceId] and races[raceId].state == "waiting" then
        races[raceId].players[player] = {
            checkpoints = 0,
            spawn = {x = 0, y = 0, z = 0},
            startTime = 0
        }
        triggerClientEvent(player, "onRaceJoin", player, races[raceId])
    end
end`}</code></pre>
      </div>
    </div>

    <!-- Features List -->
    <div class="space-y-6">
      <div
        class="bg-gray-900/30 backdrop-blur-sm rounded-xl p-6 border border-white/10"
      >
        <h3 class="text-xl font-semibold text-white mb-2">
          Powerful Scripting API
        </h3>
        <p class="text-gray-400">
          Access over 1000+ functions to control every aspect of your game
          server
        </p>
      </div>

      <div
        class="bg-gray-900/30 backdrop-blur-sm rounded-xl p-6 border border-white/10"
      >
        <h3 class="text-xl font-semibold text-white mb-2">
          Real-time Networking
        </h3>
        <p class="text-gray-400">
          Built-in client-server architecture with easy-to-use event system
        </p>
      </div>

      <div
        class="bg-gray-900/30 backdrop-blur-sm rounded-xl p-6 border border-white/10"
      >
        <h3 class="text-xl font-semibold text-white mb-2">Resource System</h3>
        <p class="text-gray-400">
          Modular resource system for easy development and distribution
        </p>
      </div>

      <a
        href="https://wiki.multitheftauto.com/wiki/Scripting_Introduction"
        class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg transition-colors duration-200"
      >
        <span>Start Learning</span>
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="w-4 h-4"
          viewBox="0 0 20 20"
          fill="currentColor"
        >
          <path
            fill-rule="evenodd"
            d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z"
            clip-rule="evenodd"></path>
        </svg>
      </a>
    </div>
  </div>
</section>

<script
  is:inline
  src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/prism.min.js"
></script>
<script
  is:inline
  src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.29.0/components/prism-lua.min.js"
></script>
<script is:inline>
  Prism.highlightAll();
</script>
